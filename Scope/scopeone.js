// console.log(name);

var name = "kanak";

if (true) {
    // if you use the var keyword this will not throw an error and if you use let this wil throw an undefined error to the console.
    // and while using var keyword you can access the lastName outside from the Scope
    let lastName = "sonowal"
    console.log(lastName);
}
// console.log(lastName);